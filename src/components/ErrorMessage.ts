import styled from 'styled-components';
import {Text} from 'react-native';

export const ErrorMessage = styled(Text)`
  font-size: 'Monrope-Light';
  margin-left: 5px;
  margin-top: 5px;
  font-size: 13px;
  color: #db4b3a;
`;
